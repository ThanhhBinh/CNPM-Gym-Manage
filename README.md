# CNPM-Gym-Manage
Starter project structure for the CNPM Gym Management repository.

## Structure
- `src/` - application source code
- `tests/` - automated tests
- `docs/` - project documentation
- `scripts/` - utility and automation scripts
- `config/` - configuration files

## Next steps
1. Choose your tech stack (e.g. Node.js, Python, Java, etc.).
2. Initialize dependency management.
3. Add a first runnable module in `src/` and matching tests in `tests/`.
